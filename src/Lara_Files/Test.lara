import weaver.WeaverJps;
import weaver.Weaver;

aspectdef test
input outputPath end

	for(var $jp of WeaverJps.root().descendants) {
		if(Weaver.isJoinPoint($jp ,"binaryExpression")){
				//if($jp.ancestor("statement").isFinal)
				var $call = $jp.ancestor("call");
				if($call !== undefined && $call.target === "null")
					println("Call "+ $call+"\n\n\n");
		} 
	}
end